# AWS Lambda Functions

These functions implement the AWS serverless backend.

## Packaging

Run the script from the repo root to create zip artifacts in dist/:

```bash
./scripts/package_lambdas.sh
```
